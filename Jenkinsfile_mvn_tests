pipeline {
    agent any
    stages {
        stage("test") {
            steps {
                script {
                  echo "The build number is ${env.BUILD_NUMBER}"
                  sh 'echo  Execution date - `date`'
                }
            }
        }
    }
}